import React from 'react';

const Environments: React.FC = () => (
  <div className="flex flex-1 items-center justify-center">
    <h1 className="text-2xl font-bold">Environments Page (stub)</h1>
  </div>
);

export default Environments; 