import React from 'react';

const RightPanel: React.FC = () => (
  <div className="w-80 bg-zinc-900 border-l border-zinc-800 flex items-center justify-center text-zinc-400 text-base">
    Collections Panel (stub)
  </div>
);

export default RightPanel; 